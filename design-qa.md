# Design QA — department header brand

Source: `codex-clipboard-dc32b934-3e4a-4357-b279-5daa6fd79552.png`

Implementation: `department.html`, local preview at 800 px viewport.

## Comparison

- The old circular flag and compact single-line label are removed.
- The lockup uses two lines: `СПОРТИВНАЯ` above `ДРАМА.`.
- The first line uses the existing deep green brand color; the second line and period use the red accent.
- Heavy serif typography, tight tracking, and compressed line height match the supplied reference.
- The mark stays left aligned and remains readable without colliding with department navigation.
- The mobile breakpoint reduces the type size and gives the lockup a full header row.

## Remaining differences

- The browser uses Georgia, the closest font already present in the product, so individual glyph shapes may differ slightly from the reference artwork.

No P0, P1, or P2 issues found.

**final result: passed**
